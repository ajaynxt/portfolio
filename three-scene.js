import * as THREE from "https://cdn.jsdelivr.net/npm/three@0.185.0/build/three.module.js";
const canvas=document.getElementById('heroCanvas'),hero=document.querySelector('.hero');if(canvas&&hero){try{const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.7));renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.05;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(34,1,.1,100);camera.position.set(0,0,12);const group=new THREE.Group();group.position.set(3.3,.2,0);scene.add(group);let accent=new THREE.Color('#d6b06f'),base=new THREE.Color('#151511');const metal=new THREE.MeshPhysicalMaterial({color:accent,metalness:.92,roughness:.18,clearcoat:1,clearcoatRoughness:.12});const dark=new THREE.MeshPhysicalMaterial({color:base,metalness:.78,roughness:.2,clearcoat:.8});const glass=new THREE.MeshPhysicalMaterial({color:accent,transparent:true,opacity:.15,transmission:.75,roughness:.06,thickness:.65,side:THREE.DoubleSide});const knot=new THREE.Mesh(new THREE.TorusKnotGeometry(1.65,.48,240,32,2,3),metal);group.add(knot);const shell=new THREE.Mesh(new THREE.IcosahedronGeometry(2.55,2),new THREE.MeshBasicMaterial({color:accent,wireframe:true,transparent:true,opacity:.25}));group.add(shell);const rings=[];[[3.2,.035,1.05,.2,.1],[2.8,.02,.3,.85,1.1],[3.65,.014,.55,-.55,.3]].forEach((r,i)=>{const m=new THREE.Mesh(new THREE.TorusGeometry(r[0],r[1],14,220),i===2?glass:metal);m.rotation.set(r[2],r[3],r[4]);group.add(m);rings.push(m)});for(let i=0;i<8;i++){const m=new THREE.Mesh(new THREE.BoxGeometry(.07,.5+(i%3)*.18,.07),i%2?dark:metal),a=i/8*Math.PI*2;m.position.set(Math.cos(a)*3.75,Math.sin(a)*2.45,(i%3-1)*.7);m.rotation.z=a;group.add(m)}const count=innerWidth<700?500:1100,pos=new Float32Array(count*3);for(let i=0;i<count;i++){pos[i*3]=(Math.random()-.5)*24;pos[i*3+1]=(Math.random()-.5)*15;pos[i*3+2]=(Math.random()-.5)*14}const geo=new THREE.BufferGeometry();geo.setAttribute('position',new THREE.BufferAttribute(pos,3));const points=new THREE.Points(geo,new THREE.PointsMaterial({color:accent,size:.018,transparent:true,opacity:.45}));scene.add(points);const key=new THREE.DirectionalLight(0xffe4b0,4.5);key.position.set(5,4,6);scene.add(key);const rim=new THREE.PointLight(0x8e6a37,24,22,2);rim.position.set(1,-4,4);scene.add(rim);scene.add(new THREE.AmbientLight(0x3a332b,1.25));let tx=0,ty=0,paused=matchMedia('(prefers-reduced-motion: reduce)').matches;addEventListener('pointermove',e=>{tx=e.clientX/innerWidth-.5;ty=e.clientY/innerHeight-.5},{passive:true});addEventListener('ajaynxt:atmosphere',e=>{const c=e.detail.id==='ivory'?'#c4432e':e.detail.id==='midnight'?'#6d9cff':e.detail.id==='gallery'?'#2a6e5d':'#d6b06f';accent.set(c);metal.color.set(c);glass.color.set(c);shell.material.color.set(c);points.material.color.set(c);key.color.set(e.detail.tone==='light'?'#fff1dc':'#ffe4b0')});function resize(){const r=hero.getBoundingClientRect();renderer.setSize(r.width,r.height,false);camera.aspect=r.width/r.height;camera.updateProjectionMatrix();group.position.x=r.width<760?1.2:3.3;group.scale.setScalar(r.width<760?.78:1)}resize();addEventListener('resize',resize);const clock=new THREE.Clock();renderer.setAnimationLoop(()=>{const t=clock.getElapsedTime();if(!paused){group.rotation.y+=(tx*.42-group.rotation.y)*.024;group.rotation.x+=(-ty*.25-group.rotation.x)*.024;knot.rotation.x=t*.09;knot.rotation.y=t*.12;shell.rotation.y=-t*.045;rings[0].rotation.z=t*.055;rings[1].rotation.x=.3-t*.037;rings[2].rotation.y=-.55+t*.025;points.rotation.y=t*.006}renderer.render(scene,camera)});hero.classList.add('webgl-ready')}catch(e){console.warn('AJAYNXT 3D fallback active',e)}}
